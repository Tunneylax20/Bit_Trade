<!DOCTYPE html>
<html>
<head>
  <title>BitTrade</title>
</head>
<body>
<header>
<%= link_to "Home", root_path %>

<% if logged_in? %>
  <p><%= link_to("Log Out #{current_user.user_name}", "/session", method: "delete") unless current_page?(root_path) %></p>  
<% else %>
  <p><%= link_to("Log In","/login") unless current_page?(root_path) %></p>
<% end %>

<p>Bitcoin Price: $<strong><%= get_current_price %></strong></p>

</header>


<%= yield %>

</body>
</html>
