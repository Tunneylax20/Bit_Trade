<!DOCTYPE html>
<html>
<head>
  <title>BitTrade</title>
  <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
</head>
<body>
<header>

  <div id="nav">
    <ul>
      <li><%= link_to "Home", root_path %></li>

      <% if logged_in? %>
        <li><%= link_to("Log Out #{current_user.user_name}", "/session", method: "delete") unless current_page?(root_path) %></li>  
      <% else %>
        <li><%= link_to("Log In","/login") unless current_page?(root_path) %></li>
      <% end %>

      <li>Bitcoin Price: $<strong><%= get_current_price %></strong></li>
    </ul>
  </div>

</header>


<%= yield %>

</body>
</html>
