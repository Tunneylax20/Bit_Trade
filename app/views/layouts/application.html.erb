<!DOCTYPE html>
<html>
<head>
  <title>BitTrade</title>
  <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
</head>
<body>
<header>

  <div class="nav">

      <% if logged_in? %>
        <p id="login"><%= link_to("Log Out #{current_user.user_name}", "/session", method: "delete") unless current_page?(root_path) %></p>  
      <% else %>
        <p id="login"><%= link_to("Log In","/login") unless current_page?(root_path) %></p>
      <% end %>

      <p id="price">Bitcoin Price: $<strong><%= get_current_price %></strong></p>
  </div>

</header>


<%= yield %>

</body>
</html>
