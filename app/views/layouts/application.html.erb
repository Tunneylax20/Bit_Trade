<!DOCTYPE html>
<html>
<head>
  <title>BitTrade</title>
  <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
</head>
<body>
<header>
<div class="container">
  <div class="navbar navbar-default blue-background">
   <% if logged_in? %>
      <span id="login"><%= link_to("Log Out #{current_user.user_name}", "/session", method: "delete") unless current_page?(root_path) %></span>  
    <% else %>
      <span id="login"><%= link_to("Log In","/login") unless current_page?(root_path) %></span>
    <% end %>

      <span>Bitcoin Price: $<%= get_current_price %></span>
      <span>Doge Price: $<%= doge_price_usd %></span>
  </div>
</div>

</header>


<%= yield %>

</body>
</html>
